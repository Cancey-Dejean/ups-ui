import Container from "@/components/atoms/Container"

type Props = {}

const Header = ({}: Props) => {
  return (
    <header>
      <Container>Header</Container>
    </header>
  )
}

export default Header
